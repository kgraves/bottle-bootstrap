bottle-bootstrap
===============

bottle.py + sqlalchemy + foundation + ???

setup
-----
pip install bottle bottle-sqlalchemy
git clone ...

usage
-----
```sh
cd bottle-bootstrap
python app.py
```

deployment
----------
fab prod deploy (in progress)

